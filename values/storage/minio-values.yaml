mode: standalone

replicas: 1

rootUser: admin

rootPassword: ChangeMeStrongPassword123!

persistence:
  enabled: true

  storageClass: longhorn

  size: 20Gi

resources:
  requests:
    cpu: 250m
    memory: 512Mi

  limits:
    cpu: "1"
    memory: 1Gi

consoleService:
  type: ClusterIP

service:
  type: ClusterIP